# Ignore everything except the schemas and backend directory
# see https://docs.docker.com/build/cache/optimize/#keep-the-context-small
/*
!/schemas
!/backend

# Ignore directory contents in the backend directory except for the worker directory
/backend/*/*
!/backend/worker/*

# Python artifacts
**/__pycache__
**/.pytest_cache
