# ODT Environment Configuration (LOCAL development)
# Copy this file to .env and fill in your values.
# See backend/config.py for all available backend options and defaults.
#
# This file is used BOTH for local development AND as the base for Docker
# (docker-compose loads .env first, then .env.docker overrides networking).

# =============================================================================
# REQUIRED SETTINGS (no defaults, must be set for full functionality)
# =============================================================================

# Helmholtz AAI OAuth2 credentials (required for authentication)
# Register at: https://login.helmholtz.de/
FLASK_HELMHOLTZ_CLIENT_ID=
FLASK_HELMHOLTZ_CLIENT_SECRET=

# =============================================================================
# OPTIONAL SETTINGS (defaults shown, uncomment to override)
# =============================================================================

# --- Flask configuration (FLASK_ prefix is stripped by Flask's from_prefixed_env) ---

# Flask secret key for session encryption (CHANGE IN PRODUCTION!)
# FLASK_SECRET_KEY=change-me-in-production

# MongoDB connection URI (local development default)
# FLASK_MONGO_URI=mongodb://localhost:27017/oligo_db

# Directory settings (relative paths)
# FLASK_RELATIVE_DATA_ACCESS_PATH=data-access
# FLASK_RELATIVE_UPLOAD_PATH=uploads
# FLASK_RELATIVE_USERDATA_PATH=user_data

# --- Celery configuration (no prefix, used directly by Celery) ---

# Celery message broker (RabbitMQ)
# CELERY_BROKER=pyamqp://guest@localhost//

# Celery result backend (MongoDB)
# CELERY_MONGO_URI=mongodb://localhost:27017/

# --- Pipeline timeout configuration ---

# Timeout mode: "config" (fixed values below) or "heuristic" (p95 of past run durations)
# PIPELINE_TIMEOUT_MODE=config

# Fixed timeout values in seconds (used in config mode, and as fallback in heuristic mode)
# PIPELINE_TIMEOUT_ANON=3600     # anonymous users: 1 hour
# PIPELINE_TIMEOUT_AUTH=7200     # authenticated users: 2 hours
# PIPELINE_TIMEOUT_HARD_MARGIN=300  # extra seconds before SIGKILL backstop

# Heuristic mode settings (only relevant when PIPELINE_TIMEOUT_MODE=heuristic)
# PIPELINE_TIMEOUT_HEURISTIC_FACTOR=3.0        # multiply p95 duration by this factor
# PIPELINE_TIMEOUT_HEURISTIC_PERCENTILE=95     # which percentile of past durations to use
# PIPELINE_TIMEOUT_HEURISTIC_WINDOW_DAYS=30    # how many days of history to consider

# --- Other settings (not part of Flask or Celery config) ---

# Frontend URL (where the React app is running)
# FRONTEND_URL=http://localhost:3000
# Backend URL (where the Flask API runs; used for OAuth redirect and by frontend/Docker)
# BACKEND_URL=http://localhost:5000

# Maximum feedback message length (used by both frontend and backend)
# FEEDBACK_MAX_LENGTH=2000
