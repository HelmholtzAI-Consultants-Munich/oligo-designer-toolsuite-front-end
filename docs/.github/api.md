---
title: API Endpoints
layout: default
nav_order: 6
---
**Known endpoints (from frontend usage):**

- `POST /api/init_run_id` → returns `{ "run_id": "..." }`
- `GET  /api/check_auth`  → returns session/user info

> Add the remaining pipeline-specific endpoints here once confirmed.
